name: modules
title: Exercises
version: master
nav:
  - modules/ROOT/nav.adoc

asciidoc:
  attributes:
    lab_name: "{{ lab_name }}"
    release-version: master
    page-pagination: true
    guid: my-guid
    ssh_user: lab-user
    ssh_password: ansible123
    ssh_command: ssh lab-user@bastion
    page-links:
    - url: https://redhat.com
      text: Red Hat
{% if environment_variables is defined %}
    environment_variables:
{% for key, value in environment_variables.items() %}
      {{ key }}: "{{ value }}"
{% endfor %}
{% endif %}
